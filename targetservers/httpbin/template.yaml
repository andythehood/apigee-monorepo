# yaml-language-server: $schema=../../.schemas/targetserver-schema.json

targetserver:
  name: httpbin32
  host: ${HOST}
  port: ${PORT}
  isEnabled: true
  sSLInfo:
    enabled: ${SSL}
    commonName: {}
    enforce: true
  protocol: http